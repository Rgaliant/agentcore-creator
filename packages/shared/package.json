{
  "name": "@agentcore/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript types and Zod schemas for agentcore-creator",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc --outDir dist",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "*"
  },
  "dependencies": {
    "zod": "^3.23.0"
  }
}
