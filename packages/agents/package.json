{
  "name": "@agentcore/agents",
  "version": "0.1.0",
  "private": true,
  "description": "Pre-built agent workflows for agentcore-creator",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@agentcore/shared": "workspace:*",
    "@aws-sdk/client-bedrock-agent-runtime": "^3.600.0",
    "@aws-sdk/client-dynamodb": "^3.600.0",
    "@aws-sdk/lib-dynamodb": "^3.600.0",
    "@aws-sdk/client-s3": "^3.600.0",
    "@octokit/rest": "^21.0.0",
    "@octokit/auth-app": "^7.0.0",
    "workflow": "^4.3.1",
    "@workflow/ai": "^5.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "typescript": "*",
    "vitest": "^2.0.0",
    "@types/node": "*"
  }
}
